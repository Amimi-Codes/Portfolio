const reducedMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;
document.getElementById('year')?.replaceChildren(String(new Date().getFullYear()));
const reveals=document.querySelectorAll('.reveal');
if('IntersectionObserver' in window){const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.12});reveals.forEach(item=>observer.observe(item))}else reveals.forEach(item=>item.classList.add('visible'));
const canvas=document.getElementById('particles');
if(canvas&&!reducedMotion){const ctx=canvas.getContext('2d');let width=0,height=0,dpr=1,particles=[];const pointer={x:-1000,y:-1000,active:false};function resize(){dpr=Math.min(devicePixelRatio||1,2);width=canvas.clientWidth;height=canvas.clientHeight;canvas.width=Math.round(width*dpr);canvas.height=Math.round(height*dpr);ctx.setTransform(dpr,0,0,dpr,0,0);const count=Math.min(135,Math.max(55,Math.floor(width*height/8500)));particles=Array.from({length:count},()=>({x:Math.random()*width,y:Math.random()*height,r:Math.random()*1.8+.5,vx:(Math.random()-.5)*.28,vy:(Math.random()-.5)*.23,phase:Math.random()*7}))}resize();window.addEventListener('resize',resize,{passive:true});canvas.parentElement.addEventListener('pointermove',event=>{const rect=canvas.getBoundingClientRect();pointer.x=event.clientX-rect.left;pointer.y=event.clientY-rect.top;pointer.active=true});canvas.parentElement.addEventListener('pointerleave',()=>pointer.active=false);function draw(time){ctx.clearRect(0,0,width,height);for(const p of particles){p.x+=p.vx;p.y+=p.vy;if(p.x<-10)p.x=width+10;if(p.x>width+10)p.x=-10;if(p.y<-10)p.y=height+10;if(p.y>height+10)p.y=-10;let dx=p.x-pointer.x,dy=p.y-pointer.y,dist=Math.hypot(dx,dy);if(pointer.active&&dist<160&&dist>0){const force=(160-dist)/160;p.x+=dx/dist*force*2.3;p.y+=dy/dist*force*2.3}const alpha=.22+.28*(.5+.5*Math.sin(time*.001+p.phase));ctx.beginPath();ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fillStyle=`rgba(198,250,232,${alpha})`;ctx.fill()}requestAnimationFrame(draw)}requestAnimationFrame(draw)}
const story=document.querySelector('.story');if(story){const steps=[...document.querySelectorAll('[data-scene]')],scene=document.querySelector('.story-stage'),progress=document.querySelector('.reading-progress span');const update=()=>{const mid=innerHeight*.5;let active=steps[0];for(const step of steps){if(step.getBoundingClientRect().top<mid)active=step}scene.dataset.active=active.dataset.scene;const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=`${Math.max(0,Math.min(100,scrollY/max*100))}%`};addEventListener('scroll',update,{passive:true});addEventListener('resize',update,{passive:true});update()}

